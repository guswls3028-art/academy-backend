# V1.12.8 Release Notes — Errata

**Errata date:** 2026-07-29 KST

## Post-seal descendant promotion

After V1.12.8 was sealed, an independently owned backend release completed
from descendant commit `9aa7f4afdb52de00ac01f40db30c5f2a86eca8e6`.
The descendant relationship to the teacher-tools boundary repair
`94ff648045f62cdcc955c8d77d39848f5bff6aad` was verified before closure, so
the promoted runtime still contains the V1.12.8 problem-solver implementation.

Backend workflow
[30457225169](https://github.com/guswls3028-art/academy-backend/actions/runs/30457225169)
completed successfully for immutable image build, isolated preproduction
migration/settings/database and health gates, production migration,
health-gated API and worker rollout, runtime digest verification, Video Batch
verification, tenant maintenance-flag verification, manifest-aware ECR
cleanup, and production lock release.

The superseding verified manifest is:

- release tag:
  `sha-9aa7f4afdb52de00ac01f40db30c5f2a86eca8e6-run-30457225169-1`;
- API:
  `sha256:428dd97dedb17ada175b572e169a0eecb11d98172e1e5090f77e7d2a92dc1fbd`;
- AI worker:
  `sha256:289883ccbd74ddb6b69f7a0843adbd67a09b96344bb74a1f717b024d244af1ef`;
- Tools worker:
  `sha256:5c8dcb3fd70de5530dbf36a58f11f69e918c24331fb3cce51a8bd3f2a7984e90`.

Post-rollout `/healthz` and `/health` returned HTTP 200 with the database
connected. Unauthenticated problem-solver create and status requests both
returned HTTP 401. The frontend remains the exact verified
`0db5353c1ba2d3382e56cb69d41062981104b474` deployment.

The authenticated no-personal-data production canary completed before this
descendant promotion, and its AI queue returned to zero with the worker ASG
returning to `desired=0`, `instances=0`. The descendant deployment changed no
teacher-tools product contract and passed the complete runtime verification
gate, so a second paid AI invocation was intentionally avoided.
