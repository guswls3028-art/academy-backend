# V1.11.35 Release Notes

**Status:** Production-deployed / CONDITIONAL GO with one controlled
verification-tool residual

**Release date:** 2026-07-28 KST

**Scope:** Atomic deployment evidence, honest verification decisions, stable
wrong-note PDF boundaries, staff fixture correctness, and production closure

## 1. Decision

CONDITIONAL GO for the released production scope.

The backend and frontend deployment workflows completed successfully, the
post-deploy production canary returned 30 PASS / 0 WARN / 0 FAIL, and the
public API health probe now records `https://api.hakwonplus.com/health` as
`OK`. The sole verification-tool warning is the local Wrangler R2 bucket-list
probe; the frontend workflow's Cloudflare deployment and production gates
passed, so this is retained as a controlled operator-tool residual rather than
reported as an application outage.

## 2. Repairs

- Make deployment histories atomic and reproducible with collision-resistant
  run IDs, required companion snapshots, source-hash stability checks,
  temporary publication, cleanup on failure, and release-manifest hash
  binding.
- Validate the release manifest from one strict UTF-8 byte snapshot and reject
  malformed types, incomplete status, unexpected image sets, invalid Git SHAs,
  and non-canonical digests.
- Propagate drift, SQS, runtime, and section WARNING/FAIL outcomes into the
  overall GO/NO-GO decision instead of allowing a false PASS.
- Record functional and worker checks as ADVISORY when the read-only verifier
  did not execute them; use the production canary or a domain E2E as the
  functional evidence.
- Read both modern AWS Batch `resourceRequirements` and legacy
  `vcpus`/`memory` job-definition shapes without creating false drift.
- Probe the operator-controlled public API URL for audit health while retaining
  the internal ALB URL as infrastructure evidence. The actual probe target is
  now emitted as `apiHealthUrl`.
- Assign `StaffWorkType` explicitly in the staff lifecycle fixture so the
  focused suite reflects the current required domain contract.
- Bound wrong-note PDF generation to 100 selected questions across UI and API,
  accept exactly 100 at the client boundary, block 101 before enqueue, extend
  the generation request timeout to 120 seconds, and keep timeout/error
  rendering null-safe.
- Correct the frontend quality-gate asset progress total from 36 to 72.

## 3. Verification

- Backend local gates: Django system check, migration dry-run, Ruff,
  submission-lifecycle boundary, touched-file/baseline boundary, and diff
  checks passed.
- Backend smoke suite: 24 passed with 5 subtests.
- Staff focused suite: 70 passed, 1 skipped, with 9 subtests.
- PowerShell verification contract and parser checks passed, including report
  collision, mixed-run, missing-companion, source-mutation, publication
  failure, manifest type-confusion, and public-health URL cases.
- Independent backend review found no actionable correctness, security, or
  compatibility finding after the final public-health correction.
- Backend deployment workflow
  [30323465196 attempt 2](https://github.com/guswls3028-art/academy-backend/actions/runs/30323465196/attempts/2):
  success, including lint, smoke, verification contracts, immutable builds,
  migration, API/AI rollout, runtime digest checks, video playback chain,
  manifest promotion, ECR cleanup, and lock release. Attempt 1's final
  verification hit a transient 180-second AWS OIDC timeout; a full rerun
  reacquired the mutation lock and completed end to end.
- Post-deploy production canary: 30 PASS, 0 WARN, 0 FAIL.
- Independent deployment verification: WARNING / CONDITIONAL GO, with only the
  local Wrangler R2 bucket-list probe warning.
- Immutable verification bundle run ID:
  `9c2b2f9fc47a4a4cb0c4373114f8ecd3`; all five companion snapshots and the
  manifest match their latest sources, and the deploy history contains the
  expected immutable-bundle appendix.
- Frontend typecheck, API/E2E safety guards, lint, and production build passed;
  486 JavaScript asset budgets were checked.
- Focused wrong-note Playwright contract: 4 passed. Exactly 100 selections are
  permitted, 101 are blocked before enqueue, a 21-second response completes,
  and timeout/null/undefined errors render safely.
- Aligned local frontend gate against the production API: 31 passed and 4
  password-mutation scenarios were policy-skipped.
- Frontend workflow
  [30320031582](https://github.com/guswls3028-art/academy-frontend/actions/runs/30320031582):
  typecheck, lint, build, Hangul companion, Cloudflare deployment, production
  canary, tenant availability, and roundtrip gates passed.
- Production frontend version and the deployed 390-pixel mobile view were
  verified at commit `81dd4fed156b5aa06f9c7d3463ad30cc51127b52`.

## 4. Production Evidence

- Backend implementation sequence:
  `2d6bca8cb6e1dc9a61a0b8b466800614cfa9d327`,
  `7db6db7f513055c73cccd80ecb6278f17d04fee4`, and
  `b3bb22a15b965527a63a86a47b61cc21b2f41789`.
- Promoted backend manifest: `f4bddc208`.
- Manifest Git SHA: `b3bb22a15b965527a63a86a47b61cc21b2f41789`.
- Manifest SHA-256:
  `88EC426849F44AFB3A8D37ACCDADBA7D5B234E65230854A1BBDA8FDCCB516C9E`.
- API digest:
  `sha256:c8bbadd4f37c59cca9ee79c73879e530928cf444bcf974b7cdc7e76bc587772a`.
- Frontend implementation and deployed version:
  `81dd4fed156b5aa06f9c7d3463ad30cc51127b52`.
- Cloudflare deployment:
  `https://44391d95.academy-frontend-26b.pages.dev`.

## 5. Documentation and Runbook Impact

- `docs/operations/formal-deploy.md` now states that
  `run-deploy-verification.ps1` is a read-only infrastructure, health, drift,
  and runtime-image collector. Functional changes require production canary or
  domain-specific E2E evidence.
- `docs/reports/README.md` now defines the immutable deployment-history bundle,
  required companions, and fail-closed behavior when a source changes or a
  companion is missing.
- The deployment evidence contract is enforced in the backend workflow smoke
  gate so future changes cannot silently regress these guarantees.

## 6. Six-Lens Audit Closure

- Backend domain: staff fixtures, Batch resource parsing, release-manifest
  validation, and report publication follow the current executable contracts.
- Tenant isolation: no tenant selectors or authorization boundaries were
  broadened; the production invariant check found no E2E/AUDIT/CHAOS residue.
- Frontend integration: the wrong-note client limit, API limit, timeout, and
  error state agree, including the exact-100/101 boundary.
- Boundary behavior: malformed manifests and incomplete/mixed report bundles
  fail closed, while empty health targets remain explicit failures.
- UX and accessibility: the wrong-note limit guidance is visible, responsive,
  and associated with the disabled create action; desktop and 375/390-pixel
  mobile layouts were checked.
- Security and operations: operator-controlled URL trust, strict manifest
  typing, production mutation locking, runtime digest verification, health,
  queues, DLQs, alarms, databases, and Batch capacity were reviewed.

Critical findings: 0. Reachable high findings: 0. No migration, runtime,
tenant-isolation, or application-health blocker remains.

## 7. Controlled Residuals

- Local `wrangler r2 bucket list` lacks usable operator credentials in the
  verification shell, so the read-only verifier reports the Front/R2/CDN
  section as WARNING. Cloudflare deployment and production frontend gates
  passed; restore/refresh Wrangler operator authentication before relying on
  this local R2 inventory probe.
- Django emits `security.W008` because TLS redirect is terminated at the ALB
  rather than by Django. The canary confirms HTTP 301 to the public HTTPS API,
  so this remains an intentional proxy-layer configuration.
- A single wrong-note generation request is intentionally limited to 100
  questions. Larger selections require the user to split the request; automatic
  chunking was not introduced in this stabilization release.
