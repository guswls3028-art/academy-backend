# V1.2.1 Release Notes — 매치업 safety 아키텍처 + 커뮤니티 전면 + 랜딩 공개 + CDN Worker

**버전:** V1.2.1
**기간:** 2026-05-01 ~ 2026-05-13 (약 2주)
**유형:** 패치 (c 변경) — 인프라 구조 변경 없음, 다수 도메인 기능 추가/안정화
**규모:** backend ~200 비-CI commit / frontend ~150 비-CI commit

> 직전 봉인 **V1.2.0 (2026-04-30)** 이후의 누적 변경.
> v1.2.0.md §22b 의 5/10-11 영상 후속은 historical 기록으로 그대로 유지하고, 본 문서에서 더 풍부한 컨텍스트로 재정리.

---

## 1. 매치업 safety 아키텍처 (Stage 0~6.x)

학원장 작성 데이터 immutable 정책 ([[domain.md §1-2]]) 위에서 **proposal-based segmentation** 으로 전환. 자동 reanalyze / bulk update 영구 폐기, 신규 AI 결과는 proposal 모델로 진입 후 어드민 승인.

### Stage 0~3 (격리 골격)
- **Stage 0**: `pending`/`page_fallback` proposal 추천 풀 진입 차단
- **Stage 2**: `selected_problem_ids` immutable guard + audit log
- **Stage 3**: `ProblemSegmentationProposal` 운영 격리 모델 + proposal_helpers (manual overlap validator) + approve/reject helpers + admin API (list/detail/approve/reject)
- **Stage 4**: 추천 풀 자격 SSOT helper + strict allowlist toggle

### Stage 5 (Tier 0 Native PDF parser)
- **Stage 5.0~5.4**: Tier 0 Native PDF parser prototype + dry-run eval runner + paper_type-aware Tier 0 v3 (anchor over-detection 87.4% 감소) + 잔존 over-detection 처리 + VLM mock contract
- **Stage 5.4.6**: `TenantSegmentationProfile` / `LayoutFingerprint` / `ManualCorrectionDelta` 빈 모델 추가
- **Stage 5.5.x**: anchor 검출 자체 개선 (학습자료 inline 차단) + analyze_pdf_v5_1 manual GT 친화 fix + Tenant Profile artifact dry-run
- **Stage 5.6~5.8**: Tier 0 plateau + fallback router dry-run + mock dispatcher integration + mock response integration
- **Stage 5.9**: proposal payload validation

### Stage 6 (Proposal 운영 통합)
- **Stage 6.0**: proposal insert adapter
- **Stage 6.2**: sandbox approve/reject guard
- **Stage 6.3 F/N/P/V**: OCR/VLM schema normalize + number_conflict 차단 + sanity tests + preprocessing input contract + LayoutFingerprint instrumentation
- **Stage 6.3A**: Proposal Review API v1 (List/Approve/Reject) — 어드민 검수 UI 백엔드
- **Stage 6.3-Pipeline**: shadow proposal generation (T1 sandbox)
- **Stage 6.3S**: manual cut CLIP 입력 raw crop 분리 + backfill 커맨드
- **Stage 6.4 prep**: T2 doc whitelist guard + smoke truncation flag
- **Stage 6.5**: manually_crop_problem 시 manual cut delta + audit log + skip_merged 카테고리 backfill
- **Stage 6.6**: V1 capture LayoutFingerprint on manual_crop + auto-only doc fingerprints backfill
- **Phase A (page-level state)**: `(auto/skip/manual)` page state 모델 + API + page_count mismatch / auto-recommend fallback fix
- **Phase C+E**: column-aware over-crop padding (2/4분할 직격) + worker bbox padding + Hybrid VLM verifier + callback page-fallback 차단 ENV flag + manual page drop + Proposal helper + pinned_ids 보호

### Hybrid VLM
- `problem_fragment` 분류 + box-area pre-filter
- V11 over-segmentation fragment 자동 합침 후처리
- `pad_y_bottom` h*7% min 12 → h*15% min 30 (1줄 안전망)
- `AutoSegmentationSnapshot` 모델 + callback hook + manual diff IoU 매칭
- Hybrid filter 가 같은 doc 의 50 cap 소진 후 RuntimeError 로 fail-soft keep 하던 구조 결함 fix

### Hit Report (적중보고서) 운영 완성
- **D-9 (arch)**: HitReport endpoints 를 `views_hit_report.py` 로 분리 + `_is_tenant_staff` import hotfix
- **submit/unsubmit 의미 정정**: submit = 학원 홈페이지 게시. P0 보호 + cache invalidation
- **share token (#67)**: 1클릭 공유 토큰 — 학생 로그인 없이 카톡 링크로 PDF (`/landing/share/<token>`)
- **share endpoint 보강**: `has_share_token` / `created` / `other_report_ids` 응답
- **#67 cycle 9**: share meta endpoint inline 카드 메타 — 2 round-trip → 1
- **#67 cycle 10**: share PDF/meta ETag 캐싱 — 변경 없으면 304
- **hot view fix**: Submit/Unsubmit select_related 누락
- **PDF iframe X-Frame-Options DENY 제거** — hover thumbnail + 상세 viewer iframe 정상화
- **landing PDF endpoint `?tenant` query fallback** — iframe raw GET / cross-tenant 404 해소
- **hit-report-toggle endpoint** — 적중보고서 한 클릭 노출 토글
- **alimtalk silent suppress** — 1인 학원 발송 케이스

### R2 audit (P0)
- **PNG compress 7곳 일괄** — cv2 level 9 → PIL default(level 6) (cv2 9는 37x slow → 워커 폭증 회피)
- **`scan_orphan_matchup_storage --tenant 필수`** + manual cut 보호 카운트 노출
- 실측 14% / 10% 감소
- JPEG q95 도입 안 함 결정 (CLIP drift + ROI $0.80/월 + 학원장 caution)
- Tenant 2 manual cut 원상태 유지 directive 영구 적용

### Tests
- **841/841 pass** — 사전 부채 28 fail + 16 error 일괄 해소
- segment_opencv CLAHE gating 회귀 + mcp gate 회귀 + dispatcher_mock test regression assertion fix
- §10 표 갱신

### V12 학습
- segmentation dataset export 커맨드 추가 + manual diff + IoU 신호 fine-tune

### Path mismatch fix (#1 cross-tenant 404)
- landing PDF endpoint `?tenant` query fallback 도입

---

## 2. 커뮤니티 전면 개편

### 신규 인프라
- **UserNotification 시스템** (#62 / #21) — 학생 헤더 종 알림 unread badge
- **UserBlock** + dynamic PWA + 신고 triage + hot keywords + 학생 배지 + 신고 row 에 target author user_id 포함 (#58)
- **Like 모델** + 좋아요 endpoint
- **Reply 답글 nesting** + 신고 + 좋아요 latency 최적화 + 정렬 ordering
- **CommunityNotification import 보강** — post_views

### API 신설
- `GET /posts/<id>/neighbors/` — prev/next 게시글 네비 (#25)
- 게시판 검색 `q` 파라미터 backend 구현 (M2 follow-up)
- my-activity 학생 endpoint + bulk-status admin endpoint (#40 #41)
- 신고함 admin + 통계 dashboard endpoints (#9 #10)
- reports pending-count + stats top_students + sanitize_legacy_replies (Phase 1/5/7)
- hero_carousel post kind public endpoint (#64 P1)

### 보안 fix (2026-05-11 ~)
- 1차 보안 리뷰 fix — visibility gate + tenant lookup + reply sanitize
- 2차 보안 리뷰 MEDIUM fix (defensive M1/M2)

### Frontend
- 학생 헤더 종 unread badge
- 좋아요·답글·신고 UI

---

## 3. 외부 공개 랜딩 + 자유게시판 + 수강후기

학원장 도메인 결정: 랜딩↔앱 트랙 분리. `apps.domains.landing_public` 신규 도메인 신설.

### Phase 1 (배포 완료, 2026-05-12)
- **모델**: PublicBoardPost / Review / Reply / Like + signals 카운트 캐시
- **API**: `/api/v1/landing-public/{board,reviews,replies,stats}` — 비로그인 read OK / family 작성 / staff 모더레이션
- **Frontend**: `/landing/board` / `/landing/reviews` list + `LandingCommunityShowcase` 통합 미리보기
- **랜딩 통합**: sticky strip "매치업"→"적중 사례" / footer 커뮤니티 컬럼 "자유게시판"/"수강 후기" 2축
- **랜딩 cafe 게시판화**: dnfm.nexon 8장 + cafe.naver 차용. fab collapsible / 작성일 / 목록 CTA push
- **adminflow**: 사이드바 / 게시판 / 매치업 카루셀 / footer / role fab

### 부수 fix
- **hero_carousel section type 등록 (#63 Phase B)** + post kind public endpoint
- **hero_images 다중 이미지 (최대 6) + hero_slot 업로드 endpoint**
- **testimonial submission**: `LandingTestimonialSubmission` 모델 + 공개/어드민 endpoint
- **consult honeypot**: `ConsultPublicView` honeypot field silent reject
- **main template 통합 + empty state 일관성** — 시각 검수 결함 2건

### Phase 2 백로그
- detail/write/like UI
- 학원장 모더레이션
- 매치업 cross-attach / HOT / verified / SEO / 사진

---

## 4. 메시징 / 알림톡 (Solapi 동기화)

### Sync 신설
- **`sync-solapi` 커맨드**: 솔라피 콘솔 → SaaS DB 일방향 검수 상태 동기화
- **sync 응답에 orphan_cleaned 표시** + 자동 청소
- **sync에서 학원장 본문 자동 덮어쓰기 제거** ([[domain.md §5]] 위반 fix)
- **sync 시 orphan solapi_template_id 자동 청소**
- **sync 버튼 안내 문구 정정** — 검수 상태만 동기화 명시

### 메시지 발송 path 정돈
- **카테고리별 적합 양식만 사용** — score 좀비 fallback 종료
- **자동발송 `message_mode = alimtalk` 단일화** (migration 0024)
- **`exam_score_published` 알림톡 자동 발송 제거** (점수 저장 시)

### 운영 도구
- **`audit_stale_templates` 커맨드** — 잔재 트리거/템플릿 식별

### Frontend 발송 모달
- 양식 선택 큰 모달 분리 + 솔라피 동기화 버튼
- 발송 모달 정돈 리팩터 — SMS 분기 제거 + DS 토큰 + CTA 단일화
- 발송 모달 헤더 정돈 — noIcon + noMinimize SSOT
- 변수 팔레트 default 접힘 → 본문 편집 영역 확대
- 양식 저장·관리 버튼 ModalFooter 좌측에 prominent
- 일괄 발송 안내 + 적용 양식 표시 — 학원장 가시성 강화
- 점수 발송 textarea 첫 학생 데이터 박힘 제거 + 일괄 발송 UX 강화
- TemplatePickerModal inline style 10건 → className 이관
- 양식 바 + 저장 액션 한 줄 통합
- 학생 드로어 상단 '📨 알림톡 발송' 버튼 prominent

---

## 5. 영상 / CDN / SECRET 보안

### CDN Worker (R2 보호)
- **신규**: HLS signed URL gateway Worker (`cdn.hakwonplus.com`) — Range/Content-Range RFC 7233 준수
- **R2 public 차단**: `pub-54ae...r2.dev` → 401 / `cdn.hakwonplus.com` signed → 200 / no-sig 401 / tampered 403 / expired 401
- **backend hot fix `aa300672`**: `CDN_HLS_BASE_URL` env 읽기 + 썸네일/HLS URL HMAC 서명 — runbook 이 가정한 SSM env 전환이 실제 코드에서 작동 안 하던 결함 fix
- **SECRET 64-char 회전**
- **인프라**: i-0c9ef95d8ae53552d docker daemon 도중 죽음 → 자가 복구

### 썸네일 SSOT 복구
- **결함**: Worker 가 R2 엔 올리지만 DB 엔 안 박던 결함 — "미처리 영상 무더기" = 썸네일 누락 (114/114 thumbnail.jpg 있음에도 DB 컬럼 빈 상태)
- **commit `c593eebf`** 배포 + migration 0015 + backfill 114/114

### SECRET_KEY 회전
- dev-secret-key (repo 공개값) → 64-char 새 SECRET_KEY 회전
- 모든 사용자 강제 로그아웃
- PowerShell JSON round-trip 로 SSM 손상 사고 + 복구 절차 기록
- `ops(infra): SSM SecureString safe-update helper` (`952d947c`) — 재발 차단

### 영상 파이프라인 잔여 (v1.2.0 §22b 후속)
- video stale DB connection after long ffmpeg under RDS Proxy fix (`f01f761d`)
- migration step env source 를 `/opt/api.env` 파일 직접 사용 (production SSOT 정합)
- migration step env dump 을 docker inspect → docker exec env (근본 fix)
- CI/CD 실패 SNS + migration drift verify + hot-deploy dead path 정리

> v1.2.0 §22b 의 5/10-11 영상 대정비 (SSL stale-conn fix / R2 병렬 / VCPU=8 / daemon·long 폐기 / cron 7종 / IaC 보강) 도 본 패치 사이클 산하. 상세는 [[v1.2.0.md#22b]].

---

## 6. 헥사고날 컷오버 §6 / §10 (대규모 이관)

[[hexagonal-cutover-policy.md]] 정책 위에서 추가 도메인 academy/ 트리 이전.

- **§6**: `worker_instance_control` 헥사고날 이관 (`0f831ecd`)
- **§10**: matchup **segmentation 12 모듈** academy 트리 일괄 이관 (`6190784c`)
- 후속: segmentation 이관 후속 — academy 내부 sibling import 절대경로 fix (`8356767a`)
- 후속: segmentation 이관 후 변수명 유지를 위한 import-as alias (`19142e3e`)
- 사전 부채 28 fail + 16 error 일괄 해소 → **841/841 pass** (`24f96831`)
- §10 표 갱신 + dispatcher_mock test regression assertion fix (`a1a3a8b4`)

---

## 7. 시험·과제 status 폐기 → 학생별 Achievement SSOT

학원장 결정: "시험 만들면 영구 운영. 학생별 진행/이수/판정." 시험 단위 OPEN/CLOSED 폐기.

- **`close_overdue_assessments` cron DISABLED** (`a0bac40e`) + 67건 일괄 OPEN 복구
- **`Exam.status=CLOSED` exclude 제거** — 학생/admin 응답 정합 (`89c06e8b`)
- **frontend status UI 전면 제거** — SessionAssessmentSidePanel / ExamHeader / ExamExplorerPage / 학생앱 exclude (`7d126692`)
- **정합성 grep 7도메인 = `Exam.status` 의존 0건** ✅

---

## 8. 합격/불합격 라벨 커스텀

학원장이 학원별 라벨 자유화 (`pass`/`fail` 외부 톤).

- **backend (`2d0a6bfc`)**: 합격/불합격 라벨 학원장 커스텀
- **frontend `useTenantLabels` hook** — PC↔모바일 어휘 SSOT 정렬 + DRY 통합
- **homework 커트라인 0점 무시 + 학생 max_score=null 대체 fallback** (`ae047997`)
- 학생앱 표기 분기

---

## 9. 학생앱 — 내 일정 휴지통 + 개별 숨김

- **지난 일정 일괄 숨김** (`cd9f3f19`) — 휴지통 아이콘
- **개별 숨김 (hide/unhide) API + `schedule_hidden_ids` 모델** (`bb6bb012`)
- **모바일 swipe-to-hide + undo banner** (`b74ac2a5`)
- 학생 차시 허브 / 헤더 종 unread badge / 사용자 영역 보호

---

## 10. 수강생 등록 / 시험 만들기 UX 정정

학원장 첫 사용자 진단. 자동 시드 폐기 (차시별 인원 변동 = 학원장 명시 통제).

- **`SessionEnrollModal`**: 직전차시 prominent CTA + 강의수강생 빠른경로 + 우측 학교·학년 chip + 이미등록 숨김 안내
- **backend `SessionEnrollmentSerializer`**: `student_school`/`student_grade` 추가 (동명이인 chip 합성)
- **시험·과제 모달 3카드 평이화 + 자동등록 toast 명시 + 차시 수강생 0명 사전 경고**
- **성적탭 워크플로우 2단계 정합 + 표시옵션 collapsed-default**

---

## 11. 성적탭 UX 대정비

학원장 호소 영역. 시각 검수 + 캡처 후속 잔존 결함 fix 다회.

- **SSOT 복구**: 강의 딱지 부활 + 출석 뱃지 SSOT + 인라인 style 토큰화 (`634c86cc`)
- **총점 변경 모달화 + 컬럼 순서 드래그앤드롭** (`4f98fddb`)
- **selectionBar 평상시 노출 + 표시옵션 toggle 통합 + 선택 해제 재배치** (`d1e440dd`)
- **TDZ ReferenceError fix** — 성적탭 페이지 전체 crash 회귀 즉시 복구 (`fdcd2ece`)
- **시험·과제 컬럼 리사이즈 가능** + 학생 시각 강화 + 양식 변수 보존
- **컬럼 폭 prefs v2 bump** — stale localStorage 무시
- **시각 polish** — 1100 viewport 정합 + 시험명 wrap 정책 + 셀 inline dot indicator
- **시험 헤더 컬럼 폭 드래그 핸들 부재 fix** — ResizableTh 로 전환
- **시험 sub-row '합산' 라벨 N번 반복 제거**
- **시험·과제 헤더 wrap** — 한국어 띄어쓰기 단위 유지 (`break-keep → break-words`)
- **합불 컬럼 제거 후 row1 총점 헤더 colSpan 정합 + 셀 간격 확장**
- **점수탭 "메시지 발송"(SMS) 버튼 제거** — 알림톡 단일 경로 통일

---

## 12. 화이트 테마 흰버튼 가시성 SSOT

학원장 반복 호소 fix. [[engineering.md]] AntD 6 border trap 영구 정책 (`4eac5725`).

- **ds-button secondary** = `Neutral100 fill + Neutral300 border`
- **ds-button ghost** = `transparent + divider border`
- **AntD `.ant-btn`/`.ant-btn-default`** 동일 정책 + `border` 단축 필수 (AntD 6 은 width 0 박음)
- **primary** = brand 직접
- **다크 4종 한정**: `:not(.ant-btn-primary):not(.ant-btn-dangerous)` 로 좁혀 Neutral200 한 단계 밝게
- **`_artifacts/btn-vis-*.png`** 12장 SSOT

---

## 13. Teacher SSOT 마이그레이션 (Phase 1+2+3)

Typography `--text-name/body/meta/hint` 도입 + 선생앱 1+2+3차 적용 + 빈박스 결함 fix.

- **commits `3772234a` (P1+2 48파일) + `2ab522c7` (P3 9파일)**
- **219 raw size → `ICON.*`** / **20 `text-[15px]` → `ds-text-name`** / **21 `size={20}` → `ICON.lg`** / **13 `size={15}` → `ICON.sm`** / **4 `size={13}` → `ICON.xs`**
- **`LectureChip` graceful null** — 빈 파란 □ 결함 사라짐
- tsc 0 / build 3회 / Cloudflare Pages 2회 success / 캡처 36장 전수 Read
- production computed CSS 직접 측정 `--icon-*/--text-*` 정확 적용
- 잔존 (Phase 4): KPI 위계 / 알림센터 모호 / text-[Npx] 421 / size 9-12 16건

---

## 14. Typography 4단계 위계 SSOT 신설

- **4단계 위계 SSOT 토큰 신설** + ds-table 전역 적용 (`d3bc69bd`)
- **모바일 페이지 헤더 + 카드 이름 영역 위계 일관** (`67086484`)
- **변종 클래스 (work/staff-home/flat/scores) font 13→14 일관성**
- **표 클래스 + 강의 딱지 + 액션 아이콘 키우기** — 학원장 호소 영역

---

## 15. ICON·CONTROL SSOT 토큰 +2~4px

나이든 학원장 가시성 보강 (`3ab904ae`).

- 시각 검수 2차 발견 영역 raw 13/14 → 16/18
- 학원장 자주 보는 페이지 raw size 12/13 → 16/18
- 모바일 메시지/커뮤니티/설정 raw size 11/12/13 → 14/16
- PC 클리닉 콘솔/설정 페이지 raw size 11/13 → 14/16

---

## 16. Submissions safety P0~P3

- **P0 orphan 재발 차단** + audit / picker / batch 보강
- **P1+P2**: 미식별 인박스 응답에 `target_resolved` 추가
- **P3**: pending API 에 `is_discarded` + `discard_reason` 노출
- **cascade signal 단위 테스트 + discard-batch 500건 상한**

---

## 17. 선생앱 강의/차시 + 결과/채점

- **enrollment endpoint URL 정정** — 404 → 정상
- **admin results endpoint 채택** + `enrollment_id` schema 정합
- **session_id filter + UX 강화** (P0 + P1 묶음)
- **ExamDetailPage / MobileScoreEntryPage enrollment fallback**
- **사이드바 성적 조회 탭 '결과 없음' 빈 응답 fix**
- **homework meta JSON schema 폴백** — 마감일/만점 미표시 fix
- **결과/채점 결함 fix + 과제 카드 메타 보강**
- **fallback skeleton 통일 + 펄스 강화 + 옵티미스틱**

---

## 18. 인프라 / 운영

### 비용 절감
- **video-ops CE instanceType m6g.medium → t4g.medium** (`722cefbc`)
- 5/11 fix 효과 실측: $24→$9/일 60% 절감
- 누적 $19+5/월 절감 (t4g rebuild 후)

### CI/CD 안정
- **`d1598ef3`**: CI/CD 실패 SNS + migration drift verify + hot-deploy dead path 정리
- **messaging worker detect-changes 에 `apps/domains/messaging/` 추가** (`3d1f87bb`)
- **migration step env source `/opt/api.env`** (production SSOT 정합)
- **migration step env dump** docker inspect → docker exec env

### CronTab / EventBridge
- enqueue cron 10→30min
- worker_instance_control 헥사고날 이관
- CloudTrail 생성
- EventBridge 중복 target 제거
- `close_overdue_assessments` DISABLED

### SSM 안전성
- **`ops(infra): SSM SecureString safe-update helper`** — PowerShell JSON 사고 재발 차단
- Dev Alerts Cron SECRET_KEY 자가 복구 확인

### Migration 복원
- **누락 migration 0038 복원** (video_max_sessions/devices + landing index rename) (`db273372`)

### Middleware 정리
- **`MustChangePasswordGate` 클래스 누락 복원** — settings reference 만 박혀 있던 잔재 정리 (`4f59af8b`)

### Docs 구조
- restructure to purpose-based folders (`a1da86c8`)
- write README index for new structure (entry + 8 sub) (`0d66f471`)
- refs 업데이트

---

## 19. 인사말 + 어휘 SSOT

- **인사말 존칭**: "유현진" → "유현진 원장님"
- **PC↔모바일 어휘 SSOT 정렬 + DRY 통합** — `useTenantLabels` hook
- **`"직원 등록"→"직원 추가"`** + StudentScoresDrawer typecheck 회복
- **수납 KPI 단어 분기** + PC 사이드바 너비 정합
- **사용자 알림 toast 명칭 통일** ("작업박스")

---

## 20. 검증 (V1.2.1 봉인 시점)

### Backend
- **테스트**: matchup 841/841 pass (사전 부채 28 fail + 16 error 해소)
- **배포**: latest `89c06e8b`, V1 Build and Push 28m47s SUCCESS, ASG refresh 진행
- **MIGRATIONS**: 0038 복원 + landing_public 신규 + autosend 0024 + schedule_hidden_ids
- **워커**: SQS=0, DLQ=0, heartbeat 정상

### Frontend
- **배포**: latest `fdcd2ece`, Frontend Quality Gate 4m14s SUCCESS, Cloudflare Pages 배포
- **typecheck**: 0 fail
- **lint**: R-11 baseline 동결 유지

### 시각 검증
- 화이트 버튼 가시성 — `_artifacts/btn-vis-*.png` 12장 SSOT
- 선생앱 SSOT migration — 캡처 36장 전수 Read + production computed CSS 직접 측정
- 성적탭 UX — 1100 viewport 정합

### 보안
- 커뮤니티 1차+2차 보안 리뷰 fix
- SECRET_KEY 64-char 회전 (모든 사용자 강제 로그아웃 + 24h 운영 정상)
- CDN Worker R2 public 차단 verification (pub-r2.dev=401)
- landing PDF `?tenant` query fallback

---

## 21. 미해결 백로그 / 다음 패치 (V1.2.2)

### 학원장 백로그 (2026-05-12 저녁) — 15+ 항목 대부분 본 패치에서 fix 완료
**P0 알림톡 영역 (이미 본 패치에서 fix 완료)**:
- ✅ 알림톡 즉발 — `policy.py:111` exam_score_published 자동 발화 차단 (`ff2a3f93`)
- ✅ dead 템플릿 — `0026_cleanup_dead_triggers` data migration + `audit_stale_templates` 커맨드 + `0024` autosend alimtalk 단일화
- ✅ score 좀비 fallback 종료 (`2cfaea34`)

**알림톡 정책 reaffirm**: **검수 통과 4종 ITEM_LIST** (`clinic_info` / `score` / `attendance` / `clinic_change`) + `#{선생님메모}` / `#{공지내용}` **본문 자유 정책** ([[domain.md §5]]) 으로 **모든 발송 케이스 cover 가능**. 신규 카카오 검수 / 신규 트리거 추가 영구 불필요.

**P0/P1/P2/P3 대부분 fix 완료** (`project_backlog_2026_05_12_evening` 메모리 Step 1 참조):
- ✅ #1 다중 생성 순서 변경, #2 과제 제목 영역 확대, #3 일괄 커트라인, #4 0점 저장, #5 합불 커스텀 (4 파일 + hook + generateScoreReport 완료, 잔여 5파일 점진), #6 미리보기 audit 인프라 3종 완료, #11/#12 enrollment matrix end-to-end, #13 성적통계 랜딩 (`PublicExamShowcase` + showcase API + LandingScoresPage + footer link), #14 매치업 종료날짜 (`HitReportShowcaseItem.published_until`), #15 매치업 학교별 카테고리 (`MatchupDocument.exam_cycle` + `exam_year` + `LandingReportsListPage` by_school sort)
- 잔여: #9 성적탭 CTA polish / #10 버튼 시각 추가 강화 / #5 잔여 5 파일 점진 (`generateScoreReport` 외 메시지 템플릿·인쇄 보고서·clinic 분리 검토)

> 상세: `~/.claude/projects/.../memory/project_backlog_2026_05_12_evening.md`

### 매치업 P-1 효과 측정
- submit=게시 통합 후 1주 재측정 — P-1 실측 baseline 비교
- T2 223 doc cut 30.5% / uncut 69.5% + submitted 보고서 0건 가설 검증

### R2 audit production 실행
- 사용자 승인 보류
- 🚨 **Tenant 2 manual cut 원상태 유지 directive** 🚨

### 영상 인코딩 알림톡
- Solapi 카카오 검수 승인 대기 (외부 절차)

### 매치업 segmentation Phase F 잔존
- 1366 카드 그리드 1열 layout (P1)

### 헥사고날 컷오버 후속
- 전수조사 후속 R-4/R-6/R-10/R-11/R-14 큰 리팩터 백로그
- R-11 4-29 5,442건 (+3,652)

### E2E_STRICT 잔여 fix (2026-04-29 1차 진단)
- CORS multi-tenant 8+ / lazyWithRetry 5 / AxiosTimeout 4 / ERR_FAILED 16
- → 정상화 후 strictBrowser 재플립

### Landing Public Phase 2
- detail/write/like UI
- 학원장 모더레이션
- 매치업 cross-attach / HOT / verified / SEO / 사진

### Teacher SSOT Phase 4
- KPI 위계
- 알림센터 모호
- text-[Npx] 421
- size 9-12 16건

### Infra
- SNS 메일 confirm (메일 미수신 허용 결정)
- segmentation freeze (5/12 worker_instance_control / CloudTrail 해소)
- 잔여 dev-* 시크릿 회전

### 백로그 SSOT
- 직전 P0/P1 잔여 (2026-04-29 risk audit) → `_artifacts/sessions/SESSION-BACKLOG-2026-04-29-risk-audit.md`

---

## 22. SSOT 갱신

| SSOT | 변경 |
|------|------|
| `releases/README.md` | v1.2.1 = CURRENT, v1.2.0 = 봉인 (§22b extension historical) |
| `CLAUDE.md` 헤더 | 현재 버전 V1.2.0 → V1.2.1 |
| `hexagonal-cutover-policy.md` §6 / §10 | worker_instance_control + matchup segmentation 12 모듈 이관 (`a1a3a8b4` 본문 갱신) |
| `memory/project_v1_2_1_seal_2026_05_13.md` | 봉인 시점 메모리 신설 + [[v1_2_0_seal_2026_04_30]] 현재 버전 마커 이전 |

다음 SSOT 들은 본 패치 사이클 *동안* 별도 commit 으로 본문 자체가 갱신됨 (본 문서는 메타 인덱스):
- `domain.md` §1-2 (매치업 immutable + manual cut 보호) — 봉인 기간 동안 정책 본문 add
- `domain.md` §5 (알림톡 본문 자유) — 본 패치에서 sync 자동 덮어쓰기 제거 fix 로 강제 적용
- `domain.md` §16 (AntD 6 border trap) — 화이트 버튼 SSOT 캡처 12장 근거 추가
- `engineering.md` Badge/Icon SSOT — Typography 4단계 위계 토큰 신설 + raw px 신규 금지 강화

---

## 23. 봉인

봉인 시점: **2026-05-13**
SHA backend: `89c06e8b`
SHA frontend: `fdcd2ece`
