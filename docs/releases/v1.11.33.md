# V1.11.33 Release Notes

**Status:** Production-deployed / GO with licensed Hangul reopen pending

**Release date:** 2026-07-28 KST

**Scope:** Warning-free Problem Studio HWPX package manifest and final
production compatibility evidence

## 1. Decision

GO for production HWPX generation.

The editor-compatible HWPX writer now explicitly references the root
`version.xml` from `Contents/content.hpf`. A fresh tenant-1, non-persisting
production result passed reopen, schema, ZIP-order, package-reference,
preview/section synchronization, and package validation with zero warnings.

The remaining device-owned check is opening this new artifact in a licensed
Hancom Hangul editor. The current verification machine does not have that
application, so the licensed-editor UAT item remains
`needs-manual-validation`.

## 2. Repair and Boundaries

- The previous production repair already replaced the partial hand-written
  HWPX ZIP/XML package with the `python-hwpx==4.2.0` compatible skeleton.
- The skeleton contained root `version.xml` but did not list it in
  `Contents/content.hpf`, so package discovery used a valid fallback and
  emitted one non-fatal warning.
- The writer now adds the standard OPF manifest reference before save and
  preserves an existing reference if a future skeleton supplies one.
- The public route, request payload, result ZIP layout, filename, review text,
  frontend, database, tenant scoping, authorization, and destructive-data
  behavior are unchanged.
- No database migration was required.

## 3. Verification

- Problem Studio regression suite: 28 passed.
- Relevant Ruff checks, Django system check, migration dry-run, dependency
  consistency, and diff checks: pass.
- Local `hwpx-validate` and `hwpx-validate-package`: pass with zero package
  warnings.
- Backend deployment workflow
  [30286385427](https://github.com/guswls3028-art/academy-backend/actions/runs/30286385427):
  success, including smoke/lint gates, immutable API/AI/tools image builds,
  rollout, runtime digest verification, and manifest promotion.
- A tenant-1, non-persisting production transfer request returned HTTP 200.
  Its 8,772-byte HWPX (`SHA-256
  cb0a1497f96a575598ba98d5e7b7a8a0de31da0f566d20ce4657a1f8945e6bf6`)
  passed package reopen, schema validation, ZIP ordering, required
  header/settings/content/version references, preview/section synchronization,
  and package validation with zero warnings.
- Post-deploy production canary: 30 PASS, 0 WARN, 0 FAIL.
- Read-only deployment verification: PASS / GO.
- Runtime image verification: PASS; the live API instance digest equals the
  promoted API digest.

## 4. Production Evidence

- Implementation:
  `c228fc52546ea8e081f94c6453d1599eaab73417`.
- Promoted image tag:
  `sha-c228fc52546ea8e081f94c6453d1599eaab73417-run-30286385427-1`.
- API digest:
  `sha256:0cd3c007d8fc2ee6da8a2b3e877fbcac3a350fad53c23d8be62685546725dd4c`.
- AI CPU worker digest:
  `sha256:4d56c636a30ccb513471065b319a97ceb35aa5e8afad411db8d064d4fc107efd`.
- Tools worker digest:
  `sha256:22172866e88d63c7ed0fd214ebce5ef982106b552389f894963450511f06bc38`.
- Dated verification:
  `docs/reports/problem-studio-hwpx-verification-2026-07-28.md`.

## 5. Six-Lens Audit Closure

- Backend domain: the writer changes only the package manifest and preserves
  the existing transfer-document contract.
- Tenant isolation: authentication, tenant derivation, storage prefixes, and
  query scoping are unchanged; verification used the approved tenant 1.
- Frontend integration: no frontend or download-flow change was required.
- Boundary behavior: duplicate version references are prevented; empty,
  OCR-fed, and structured review text keep the existing generation path.
- UX and accessibility: the downloaded filename and teacher workflow are
  unchanged; the repair targets editor acceptance rather than adding a new UI.
- Security: no new parser input, network boundary, secret, permission, or
  persistence behavior was introduced.

Critical findings: 0. High findings: 0. No code, migration, runtime, or deploy
blocker remains.

## 6. Licensed Hangul Handoff

On a Windows device with licensed Hancom Hangul, open the retained
`03_자체양식_문제검수본.hwpx` from the 2026-07-28 production artifact. Record the
Hangul build, whether the damaged-file dialog is absent, and the visible
text/layout result. Do not reuse a pre-fix ZIP when closing this check.
