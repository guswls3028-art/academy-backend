# V1.11.22 Errata — Ymath Exam 604 Operational Repair

**Recorded:** 2026-07-27 KST

This errata records the authorized customer-data repair completed after the
v1.11.22 release was sealed. It does not alter the historical release decision
or application binaries.

## Repair

- The operator explicitly authorized direct repair of Ymath exam `604`.
- The precondition snapshot confirmed an active regular exam with
  `max_score=24`, no sheet, zero questions, zero answer keys, and zero stored
  results.
- Under that unchanged precondition, one `MAIN` sheet and questions `1..24`
  were created atomically.
- All 24 questions use a score of `1.0`; the persisted score sum is `24.0`.
- No answer key, result, attempt, enrollment, or student record was created or
  changed.

## Production Proof

- The persisted sheet is `255`, with `total_questions=24`,
  `choice_count=24`, and `essay_count=0`.
- A tenant-bound Ymath staff token called the real production endpoint:
  `GET /api/v1/results/admin/exams/604/result-import/template/`.
- The endpoint returned HTTP 200 with the official XLSX MIME type,
  `Content-Disposition: attachment; filename="exam_604_results.xlsx"`,
  12,780 bytes, the `PK` ZIP signature, and a valid workbook archive.
- Temporary verification files were removed and their absence was confirmed.

The previously recorded data-setup handoff is therefore resolved.
