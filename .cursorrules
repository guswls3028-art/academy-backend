# Academy 프로젝트 규칙

## 인프라 전략
- **AWS:** RDS(PostgreSQL)와 DynamoDB만 사용함.
- **Cloudflare:** 파일 저장(R2), CDN, DNS, 보안은 모두 Cloudflare를 사용함. (S3 사용 금지)
- **인증:** AWS 명령어 실행 시 반드시 `--profile default`를 붙이거나 환경변수를 정리할 것.
