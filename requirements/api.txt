# api.txt — API 전용 (표현 계층)
-r ./common.txt

# Django REST / API
djangorestframework
django-filter
django-cors-headers>=4.0.0
django-timezone-field

# Auth / Security
djangorestframework_simplejwt
PyJWT

# API Docs
drf-yasg
uritemplate
inflection

# upload_complete ffprobe 검증 (R2 presigned URL)
ffmpeg-python

# 매치업 수동 크롭: 백엔드가 PDF 페이지 렌더링 + 영역 crop을 동기 처리.
# (워커는 worker-ai-tools.txt에서 별도 설치)
PyMuPDF==1.25.3

# Server
gunicorn
gevent>=23.0.0

reportlab

# Solapi (support.messaging views)
solapi>=5.0.0
